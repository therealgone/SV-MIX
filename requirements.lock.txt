# Exact versions verified working on this machine (Windows 11, Python 3.12).
#
# WHY THIS FILE EXISTS:
# Windows Smart App Control (enforced on this PC) blocks unsigned .pyd
# binaries that lack Microsoft cloud reputation. The newest numpy/scipy/
# librosa wheels are too recent to have that reputation yet, so they fail
# at import with:
#     ImportError: DLL load failed ... An Application Control policy has
#     blocked this file.
# These older, widely-deployed builds are allowed and satisfy every
# constraint in requirements.txt.
#
# Install with:   pip install -r requirements.lock.txt
# (Plain `pip install -r requirements.txt` resolves to newer wheels and
#  will reintroduce the Smart App Control failure on this machine.)
#
# ffmpeg is still required separately and must be on PATH.

audioread==3.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cloudpickle==3.1.2
decorator==5.3.1
idna==3.19
joblib==1.6.0
lazy-loader==0.5
librosa==0.10.2.post1
llvmlite==0.43.0
msgpack==1.2.2
narwhals==2.25.0
numba==0.60.0
numpy==1.26.4
packaging==26.3
platformdirs==4.11.7
pooch==1.9.0
pycparser==3.0
pydub==0.25.1
PySide6==6.11.2
PySide6_Addons==6.11.2
PySide6_Essentials==6.11.2
requests==2.34.2
scikit-learn==1.5.2
scipy==1.13.1
shiboken6==6.11.2
soundfile==0.12.1
soxr==1.1.0
threadpoolctl==3.6.0
typing_extensions==4.16.0
urllib3==2.7.0
